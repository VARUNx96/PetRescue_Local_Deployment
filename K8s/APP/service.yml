apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: petrescue-ns
spec:
  type: NodePort
  selector:
    app: petrescue-app
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30001